= form_for :comment, remote: true, url: comments_path(commentable_type: commentable_type, commentable_id: commentable_id) do |f|

  = f.label :body

  %br/

  = f.text_area :body

  %br/

  = f.submit 'Create a comment'