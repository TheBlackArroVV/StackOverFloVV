%div.errors

= nested_form_for [@question, answer], url: question_answers_path(@question.id), remote: true do |f|

  = f.label :body
  = f.text_field :body

  %p
    Attachments:
    = render 'attachments/form', f: f

  = f.submit "#{answer.new_record? ? 'Create answer' : 'Update answer'}"
